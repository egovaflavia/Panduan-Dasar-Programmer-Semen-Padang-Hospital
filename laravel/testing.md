# Testing

* Gunakan `php artisan make:test` untuk membuat test.
* Gunakan `RefreshDatabase` untuk isolasi.
* Nama test harus deskriptif:

```php
public function test_user_can_register_with_valid_data()
```
