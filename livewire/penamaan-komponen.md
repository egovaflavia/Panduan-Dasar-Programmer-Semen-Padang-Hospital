# Penamaan Komponen

* Gunakan PascalCase untuk nama class.
* Gunakan snake-case (folder/filename) untuk blade view.

```bash
php artisan make:livewire User/Create
```

Akan menghasilkan:

* `User/Create.php`
* `user/create.blade.php`
