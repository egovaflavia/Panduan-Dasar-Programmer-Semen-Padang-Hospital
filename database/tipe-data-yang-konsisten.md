# Tipe Data yang Konsisten

* Gunakan `VARCHAR(n)` untuk teks.
* Gunakan `INT` untuk angka.
* Gunakan `DATETIME` untuk tanggal/waktu.
* Gunakan `TINYINT` (MySQL) atau `BIT` (SQL Server) untuk status boolean.
* Gunakan `DECIMAL(10,2)` untuk nilai uang.
