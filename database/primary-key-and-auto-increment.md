# Primary Key & Auto Increment

* Setiap tabel wajib memiliki **primary key**.
* Gunakan kolom `id` sebagai **primary key** otomatis:
  * **MySQL:** `AUTO_INCREMENT`
  * **SQL Server:** `IDENTITY(1,1)`
